# Port for the Express backend server to listen on
PORT=5000

# Database Connection Pool Settings
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_root_password_here
DB_NAME=hr_hiring_system

# JWT Configuration (Required in Production)
# Ensure to generate a secure, high-entropy secret string for production deployment.
JWT_SECRET=your_jwt_secret_key_here

# Demo bypassing credentials config (Optional, defaults to false)
# Set to true only in development/staging for automated login bypasses.
DEMO_MODE=false

# Frontend URL (CORS Production Origin, e.g. https://your-app.vercel.app)
FRONTEND_URL=



